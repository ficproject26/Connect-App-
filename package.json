{
  "name": "connect-app-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:backend": "npm install --prefix backend && npm run build --prefix backend",
    "start:backend": "npm start --prefix backend"
  }
}
